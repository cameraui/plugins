# Apple LLM

Brings Apple's on-device foundation model into camera.ui as an assistant model. The model answers on the Mac this plugin runs on, without an API key and without sending anything to a cloud. Needs macOS 27 on Apple Silicon with Apple Intelligence turned on, and `sudo fm license` agreed once per Mac.
