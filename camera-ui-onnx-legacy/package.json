{
  "displayName": "ONNX Legacy",
  "name": "@camera.ui/camera-ui-onnx-legacy",
  "version": "1.2.8",
  "description": "ONNX Runtime detection backend for camera.ui with the older CUDA 12 runtime. For NVIDIA GPUs before GTX 1650 (Maxwell, Pascal, Volta) and for systems that stay on CUDA 12. Use the regular ONNX plugin on newer hardware.",
  "author": "seydx (https://github.com/cameraui/plugins)",
  "main": "./bundle/dist/main.py",
  "type": "module",
  "scripts": {
    "build": "npm run sync && mypy src",
    "bundle": "npm run format && npm run lint && npm run build && cui bundle",
    "bundle:dev": "npm run format && npm run lint && npm run build && cross-env MODE=development cui bundle",
    "format": "ruff format",
    "install-updates": "npm i --save --force",
    "lint": "npm run sync && ruff check --fix",
    "prepublishOnly": "node -e \"if(!process.env.SAFE_PUBLISH){console.error('Error: Please use @camera.ui/cli to publish the plugin:\\n  npm run publish:alpha\\n  npm run publish:beta\\n  npm run publish:latest\\n');process.exit(1)}\"",
    "publish:alpha": "npm i --save --force && npm run bundle && cui publish --alpha",
    "publish:beta": "npm i --save --force && npm run bundle && cui publish --beta",
    "publish:latest": "npm i --save --force && npm run bundle && cui publish --latest",
    "sync": "node scripts/sync.mjs"
  },
  "devDependencies": {
    "@camera.ui/cli": "^0.0.87",
    "@camera.ui/sdk": "^1.2.4",
    "cross-env": "^10.1.0"
  },
  "bugs": {
    "url": "https://github.com/cameraui/plugins/issues"
  },
  "engines": {
    "camera.ui": ">=2.1.9",
    "node": ">=22.0.0"
  },
  "os": [
    "linux",
    "win32"
  ],
  "cpu": [
    "x64"
  ],
  "homepage": "https://github.com/cameraui/plugins/tree/main/camera-ui-onnx-legacy#readme",
  "keywords": [
    "camera-ui-plugin",
    "object",
    "clip-detection",
    "clip",
    "face-detection",
    "face",
    "license-plate",
    "lpd",
    "ocr",
    "alpr",
    "onnx",
    "cuda",
    "legacy",
    "detection"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/cameraui/plugins.git",
    "directory": "camera-ui-onnx-legacy"
  }
}
