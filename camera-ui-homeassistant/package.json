{
  "displayName": "Home Assistant",
  "name": "@camera.ui/camera-ui-homeassistant",
  "version": "1.0.10",
  "description": "Imports Home Assistant sensors into camera.ui: motion, occupancy, contacts, doorbells, smoke, leak and more become assignable camera.ui sensors that can trigger camera detection.",
  "author": "seydx (https://github.com/cameraui/plugins)",
  "main": "./bundle/dist/index.js",
  "type": "module",
  "scripts": {
    "build": "rimraf dist && tsc",
    "bundle": "npm run format && npm run lint && npm run build && cui bundle",
    "bundle:dev": "npm run format && npm run lint && npm run build && cross-env MODE=development cui bundle",
    "format": "prettier --write \"src/\" --ignore-unknown --no-error-on-unmatched-pattern",
    "install-updates": "npm i --save --force",
    "lint": "eslint --fix .",
    "prepublishOnly": "node -e \"if(!process.env.SAFE_PUBLISH){console.error('Error: Please use @camera.ui/cli to publish the plugin:\\n  npm run publish:alpha\\n  npm run publish:beta\\n  npm run publish:latest\\n');process.exit(1)}\"",
    "publish:alpha": "npm i --save --force && npm run bundle && cui publish --alpha",
    "publish:beta": "npm i --save --force && npm run bundle && cui publish --beta",
    "publish:latest": "npm i --save --force && npm run bundle && cui publish --latest",
    "update": "updates --update ./"
  },
  "dependencies": {
    "ws": "^8.21.3"
  },
  "devDependencies": {
    "@camera.ui/cli": "^0.0.100",
    "@camera.ui/sdk": "^1.2.32",
    "@stylistic/eslint-plugin": "^5.10.0",
    "@types/node": "26.3.0",
    "@types/ws": "^8.18.1",
    "@typescript-eslint/parser": "^8.68.0",
    "cross-env": "^10.1.0",
    "eslint": "9.39.2",
    "globals": "^17.11.0",
    "prettier": "^3.9.6",
    "rimraf": "^6.1.3",
    "tsx": "^4.23.12",
    "typescript": "5.9.3",
    "typescript-eslint": "^8.68.0",
    "updates": "^18.0.7"
  },
  "bugs": {
    "url": "https://github.com/cameraui/plugins/issues"
  },
  "engines": {
    "camera.ui": ">=2.1.10",
    "node": ">=22.0.0"
  },
  "os": [],
  "cpu": [],
  "homepage": "https://github.com/cameraui/plugins/tree/main/camera-ui-homeassistant#readme",
  "keywords": [
    "camera-ui-plugin",
    "home-assistant",
    "sensors",
    "smart-home"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/cameraui/plugins.git",
    "directory": "camera-ui-homeassistant"
  }
}
